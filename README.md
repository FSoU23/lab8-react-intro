# Лабораторная работа №8. Введение в React + JSX

Простое и функциональное приложение для управления задачами, созданное с использованием React и Vite.

## 📸 Скриншоты приложения

<img width="2534" height="1386" alt="image" src="https://github.com/user-attachments/assets/5b9fd209-13c5-4b83-b09f-b03b045a772d" />

<img width="2523" height="1399" alt="image" src="https://github.com/user-attachments/assets/a4ae6555-11ea-4081-8d15-3b7c12ad5ed4" />

## ✨ Возможности

- ➕ Добавление новых задач
- ❌ Удаление отдельных задач
- 🗑️ Очистка всего списка задач
- 💾 Автоматическое сохранение в localStorage
- 🎨 Современный и адаптивный дизайн с Tailwind CSS
- ⚡ Быстрая работа благодаря Vite

## 📚 Основные концепции React

### Что такое компонент?
**Компонент** — это независимый, переиспользуемый блок кода, который возвращает React-элементы для отображения на странице. Компоненты позволяют разбить UI на изолированные части. В нашем проекте есть 4 компонента:
- `App.jsx` — главный компонент, управляющий состоянием всего приложения
- `AddTaskForm.jsx` — компонент формы для добавления задач
- `ToDoList.jsx` — компонент списка задач
- `ToDoItem.jsx` — компонент отдельной задачи

### Что такое пропсы (props)?
**Пропсы** — это параметры, которые передаются от родительского компонента к дочернему. Они позволяют компонентам обмениваться данными. Например:
```jsx
<AddTaskForm addTask={addTask} />
```
Здесь функция `addTask` передаётся как проп в компонент `AddTaskForm`.

### Что такое состояние (state)?
**Состояние** — это данные, которые компонент хранит и может изменять. При изменении состояния компонент автоматически перерисовывается. В нашем приложении состояние используется для хранения массива задач:
```jsx
const [tasks, setTasks] = useState([]);
```

## 🤖 Использование ИИ в разработке

**Какую часть кода помог написать ИИ:**
- Структуру всех компонентов (`App.jsx`, `AddTaskForm.jsx`, `ToDoList.jsx`, `ToDoItem.jsx`)
- Логику работы с `useState` и `useEffect`
- Реализацию сохранения данных в `localStorage`
- Стилизацию с помощью Tailwind CSS
- Функционал добавления, удаления и очистки задач

ИИ предоставил готовые шаблоны кода, которые я адаптировал под требования лабораторной работы.

## 💡 Понимание компонентного подхода

**Что я понял о компонентном подходе:**

1. **Модульность** — каждый компонент отвечает за свою задачу. `ToDoItem` только отображает задачу, `AddTaskForm` только добавляет её.

2. **Переиспользуемость** — компонент `ToDoItem` используется для каждой задачи в списке, не дублируя код.

3. **Однонаправленный поток данных** — данные передаются от родителя к детям через пропсы, а дочерние компоненты могут вызывать функции родителя для изменения состояния.

4. **Разделение ответственности** — логика (состояние и функции) находится в `App.jsx`, а представление (UI) разбито на отдельные компоненты.

5. **Простота поддержки** — если нужно изменить внешний вид кнопки удаления, я редактирую только `ToDoItem.jsx`, не трогая остальной код.

Компонентный подход делает код более организованным, понятным и удобным для масштабирования.

## 🚀 Запуск проекта

### Установка зависимостей:
```bash
npm install
```

### Запуск в режиме разработки:
```bash
npm run dev
```

### Сборка для продакшена:
```bash
npm run build
```

## 🛠️ Технологии

- **React** — библиотека для создания пользовательских интерфейсов
- **Vite** — быстрый инструмент сборки
- **Tailwind CSS** — утилитарный CSS-фреймворк для стилизации
- **localStorage** — для сохранения задач в браузере

## 📂 Структура проекта
```
src/
├── App.jsx          # Главный компонент (управление состоянием)
├── AddTaskForm.jsx  # Форма добавления задач
├── ToDoList.jsx     # Список задач
└── ToDoItem.jsx     # Отдельная задача
```

## 👨‍💻 Автор

Создано в рамках лабораторной работы №8 по изучению React

## 📝 Лицензия

MIT
